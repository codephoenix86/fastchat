apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
spec:
  clusterIP: None
  selector:
    app: fastchat-mongodb
    db: mongodb
  ports:
  - protocol: TCP
    port: 27017
    targetPort: 27017