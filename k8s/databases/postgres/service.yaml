apiVersion: v1
kind: Service
metadata:
  name: postgres-service
spec:
  clusterIP: None
  selector:
    app: fastchat-postgres
    db: postgres
  ports:
  - protocol: TCP
    port: 5432
    targetPort: 5432