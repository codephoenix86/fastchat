apiVersion: v1
kind: Service
metadata:
  name: fastchat-service
spec:
  selector:
    app: fastchat-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
    nodePort: 30007
  type: NodePort